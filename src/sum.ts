/**
 * Already well-tested — see sum.test.ts. Here so the demo has at least one
 * file at 100% coverage to show contrast in the dashboard.
 */
export function sum(a: number, b: number): number {
  return a + b;
}
